import theme from "styled-theming"

export const backgroundColor = theme("mode", {
  light: "#FFF",
  dark: "#222",
})

export const textColor = theme("mode", {
  light: "#000",
  dark: "#fff",
})

export const titleColor = theme("mode", {
  light: "#000",
  dark: "#fff",
})

export const titleWrapperColor = theme("mode", {
  light: "#f9f9f9",
  dark: "#2d2d2d",
})

export const borderColor = theme("mode", {
  light: "#EBEBEB",
  dark: "#383838",
})

export const defaultButtonColor = theme("mode", {
  light: "#FFF",
  dark: "#2A2A2A",
})

export const selectedButtonColor = theme("mode", {
  light: "#2A2A2A",
  dark: "#FFFFFF",
})

export const selectedButtonTextColor = theme("mode", {
  light: "#FFFFFF",
  dark: "#2A2A2A",
})
export const defaultButtonTextColor = theme("mode", {
  light: "#2A2A2A",
  dark: "#FFFFFF",
})

export const tooltipTextColor = theme("mode", {
  light: "",
  dark: "",
})

export const tableRowHoverColor = theme("mode", {
  light: "#f8f8f8",
  dark: "#2d2d2d",
})

export const dataColumnTextColor = theme("mode", {
  light: "#2A2A2A",
  dark: "#9a9898",
})

export const scrollBarColor = theme("mode", {
  light: "rgba(0, 0, 0, 0.5)",
  dark: "#FFFFFF",
})

export const customTableOptionBackgroundColor = theme("mode", {
  light: "#eff2f5",
  dark: "transparent",
})

export const customTableOptionTextColor = theme("mode", {
  light: "gray",
  dark: "#FFF",
})

export const customTableOptionBorderColor = theme("mode", {
  light: "unset",
  dark: "#ffffff4d",
})

export const customTableSelectedAreaColor = theme("mode", {
  light: "#e8e8e8",
  dark: "#383838",
})
